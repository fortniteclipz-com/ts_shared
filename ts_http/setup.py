from setuptools import setup, find_packages

setup(
    name='ts_http',
    version='0.1',
    description='ts_http',
    author='Ahuja',
    license='',
    packages=find_packages(),
    install_requires=[],
    zip_safe=False
)
