from setuptools import setup, find_packages

setup(
    name='ts_file',
    version='0.1',
    description='ts_file',
    author='Ahuja',
    license='',
    packages=find_packages(),
    install_requires=[],
    zip_safe=False
)
