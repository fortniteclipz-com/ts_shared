from setuptools import setup, find_packages

setup(
    name='ts_model',
    version='0.1',
    description='ts_model',
    author='Ahuja',
    license='',
    packages=find_packages(),
    install_requires=[],
    zip_safe=False
)
